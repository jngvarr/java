spring:
  application:
    name: web
  security:
    user:
      name: user
      password: pass

  cloud:
    openfeign:
      client:
        config:
          clients:
            url: http://localhost:8081
feign:
  client:
    config:
      clients:
        basic-auth:
          username: user
          password: pass
#server:
#  error:
#    whitelabel:
#      enabled : false
logging:
  level:
    ru.jngvarr: DEBUG